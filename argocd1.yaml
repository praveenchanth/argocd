apiVersion: apps/v1
kind: Deployment
metadata:
  name: snapchat
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devops1
  template:
    metadata:
      labels:
        app: devops1
    spec:
      containers:
      - name: nginx
        image: nginx:1.14.2
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: snapchat
spec:
  type: ClusterIP
  selector:
    app: devops
  ports:
  - port: 80
    targetPort: 80
